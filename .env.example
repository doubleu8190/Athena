# ============================================================
# Athena 配置示例 — 复制为 .env 后按需修改
# ============================================================

# --- Server ---
HOST=127.0.0.1
PORT=8000
DEBUG=true

# --- LLM Provider ---
# 可选: openai / anthropic / deepseek / ollama
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o
LLM_API_KEY=sk-your-api-key-here
# OpenAI 默认留空；DeepSeek 默认 https://api.deepseek.com；Ollama 默认 http://localhost:11434
LLM_BASE_URL=
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4096

# --- Database ---
SQLITE_DB_PATH=./data/athena.db
CHROMADB_PATH=./data/chromadb

# --- Harness ---
MAX_TURNS_PER_RUN=20
RETRY_BUDGET=3
TOOL_TIMEOUT=60
APPROVAL_TIMEOUT=120

# --- Memory ---
SUMMARY_THRESHOLD=10
MEMORY_SYNC_INTERVAL=900
MEMORY_MIN_SCORE=0.7
MEMORY_TTL_DAYS=90
VECTOR_WEIGHT=0.6
KEYWORD_WEIGHT=0.4
RRF_K=60
RETRIEVAL_TOP_K=5
MEMORY_MAX_TOKENS=2000

# --- Context Compression ---
MAX_CONTEXT_TOKENS=128000
COMPRESSION_THRESHOLD=0.8
KEEP_RECENT_TURNS=3
MAX_SUMMARY_TOKENS=2000
SUMMARY_INCREMENTAL=true
SAVE_SUMMARY_TO_MEMORY=true

# --- Docker Sandbox (可选，默认禁用) ---
SANDBOX_ENABLED=false
SANDBOX_IMAGE=athena-sandbox:latest
SANDBOX_NETWORK_DISABLED=true
SANDBOX_MEMORY_LIMIT=256m
SANDBOX_CPU_LIMIT=0.5
SANDBOX_WORKSPACE_PATH=/workspace
SANDBOX_CHECK_INTERVAL=300
# 允许挂载的宿主路径白名单（逗号分隔）
SANDBOX_ALLOWED_PATHS=

# --- Approval ---
APPROVAL_BATCH_MODE=sequential
APPROVAL_KEYBOARD_SHORTCUTS=true
APPROVAL_SOUND_ALERT=false
